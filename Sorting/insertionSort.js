function insertionSort(array){
  var length = array.length;
  for (var i = 1; i < length; i++) {
    if (array[i - 1] > array[i]) {
      var temp = array[i];
      var lastindex = i - 1;
      for (var j = i; j == 0; j--) {
        if (array[j] > temp) {

        }
      }
    }
  }
}